import 'package:flutter/material.dart';

class FarmerRegistrationPage extends StatelessWidget {
  const FarmerRegistrationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Farmer Registration'), centerTitle: true),
      body: Center(child: Text('Farmer Registration Form Goes Here')),
    );
  }
}
